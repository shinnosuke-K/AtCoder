name=""
num=""

start:
	mkdir ./${name}/${num}
	touch ./${name}/${num}/A.py
	touch ./${name}/${num}/B.py
	touch ./${name}/${num}/C.py
	touch ./${name}/${num}/D.py
	touch ./${name}/${num}/E.py
	touch ./${name}/${num}/F.py
